name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
      
      - name: Build with PyInstaller
        run: |
          pyinstaller --name="MathConverter-Mac" \
            --windowed \
            --onefile \
            --add-data "src:src" \
            --hidden-import=PySide6 \
            --hidden-import=pyperclip \
            src/unified_gui.py
      
      - name: Create DMG
        run: |
          mkdir -p dist/MathConverter
          cp dist/MathConverter-Mac.app dist/MathConverter/ || cp dist/MathConverter-Mac dist/MathConverter/
          cp README.md dist/MathConverter/
          hdiutil create -volname "MathConverter" -srcfolder dist/MathConverter -ov -format UDZO dist/MathConverter-Mac.dmg
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: mac-build
          path: dist/MathConverter-Mac.dmg

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
      
      - name: Build with PyInstaller
        run: |
          pyinstaller --name="MathConverter-Win" `
            --windowed `
            --onefile `
            --add-data "src;src" `
            --hidden-import=PySide6 `
            --hidden-import=pyperclip `
            src/unified_gui.py
      
      - name: Create ZIP
        run: |
          Compress-Archive -Path dist/MathConverter-Win.exe,README.md -DestinationPath dist/MathConverter-Windows.zip
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: dist/MathConverter-Windows.zip

  release:
    needs: [build-mac, build-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Download Mac build
        uses: actions/download-artifact@v4
        with:
          name: mac-build
      
      - name: Download Windows build
        uses: actions/download-artifact@v4
        with:
          name: windows-build
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            MathConverter-Mac.dmg
            MathConverter-Windows.zip
          body: |
            ## ダウンロード
            
            - **Mac**: MathConverter-Mac.dmg
            - **Windows**: MathConverter-Windows.zip
            
            ## 変更内容
            
            - テンプレート機能
            - クリップボード対応
            - 統合GUI
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
